name: Secret Scanning

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
  schedule:
    # Run weekly on Fridays at 10 AM UTC
    - cron: '0 10 * * 5'
  workflow_dispatch:

concurrency:
  group: secret-scan-${{ github.ref }}
  cancel-in-progress: true

jobs:
  trufflehog:
    name: TruffleHog Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 0  # Full history for comprehensive scanning

      - name: TruffleHog OSS
        uses: trufflesecurity/trufflehog@36d680a074f34a8ec9ae32614ace45c34ec820c5 # main
        with:
          path: ./
          extra_args: --only-verified

  summary:
    name: Scan Summary
    runs-on: ubuntu-latest
    needs: [trufflehog]
    if: always()
    steps:
      - name: Check scan results
        run: |
          echo "Secret scanning completed"
          echo "TruffleHog: ${{ needs.trufflehog.result }}"
          echo "Trivy: ${{ needs.trivy-secret-scan.result }}"

          if [[ "${{ needs.trufflehog.result }}" == "failure" ]] || \
             [[ "${{ needs.trivy-secret-scan.result }}" == "failure" ]]; then
            echo "⚠️ Secret scanning detected issues"
            exit 1
          fi

          echo "✅ All secret scans passed"
