name: BalenaCloud Deploy
on:
  push:
    branches: [ main ]
  schedule:
    - cron: '30 9 1 * *'
  workflow_dispatch:

concurrency:
  group: balena-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

    - name: Create .env file
      run: |
        cat > .env << EOF
        TUNNEL_TOKEN=${{ secrets.TUNNEL_TOKEN }}
        EOF

    - name: Deploy to BalenaCloud
      uses: balena-io/deploy-to-balena-action@578f912fe5172b3147c850fc3f1327b006598b68 # master
      id: build
      with:
        balena_token: ${{ secrets.BALENA_API_TOKEN }}
        fleet: ${{ secrets.BALENA_APP_NAME }}

    - name: Log release ID
      run: echo "Built release ID ${{ steps.build.outputs.release_id }}"

